.post-detail
  .post-image
    = image_tag @post.images[0].src.to_s
  .post_name
    = @post.name
  .item-button-container
    .item-button-container__left
      = render partial: 'likes/like', locals: { post: @post, posts: @posts, likes: @likes, like: @like}
